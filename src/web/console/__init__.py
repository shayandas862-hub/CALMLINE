"""The v3 operational console (phase 5) — a NEW app beside the old demo.

Serves the three role screens and wires together the phases-1–4 backend: the
record book, the agent toolbox + orchestrator (on a deterministic offline
stand-in), the case queue + human-gated approval, and role sessions. Runs fully
offline — no database, no API keys.
"""
