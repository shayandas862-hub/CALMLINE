"""The identity gate — verification before disclosure.

Nothing personal is disclosed until a human-confirmed, machine-recorded
verification exists (`05-OPS:2.4`). This package holds the question set, the
gate that turns a handler's confirmation into a ``VerificationRecord``, the
append-only event log that proves no disclosure preceded one, and the
third-party authority path.
"""
